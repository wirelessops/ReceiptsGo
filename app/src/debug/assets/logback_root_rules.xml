<included>
    <!-- Write DEBUG (and higher-level) messages to logcat and the log file -->
    <root level="DEBUG">
        <appender-ref ref="logcat" />
        <appender-ref ref="file" />
    </root>
</included>